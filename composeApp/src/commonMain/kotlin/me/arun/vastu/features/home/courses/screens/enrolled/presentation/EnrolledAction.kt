package me.arun.vastu.features.home.courses.screens.enrolled.presentation

/**
 * Defines the actions that can be sent from the UI to the ViewModel
 * for the Enrolled feature.
 */
sealed interface EnrolledAction {
    // Example: data class ButtonClicked(val itemId: String) : EnrolledAction
}